package com.example.pfe.enums;

public enum LeaveType {
    ANNUAL,   // Congé annuel   — 22 days default
    SICK,     // Congé maladie  — 15 days default
    UNPAID    // Congé sans solde — 0 days default
}